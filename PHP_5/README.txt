Carter Fowler

1. Search bar to better accommodate larger databases (with semi-fuzzy search/autocompletion).

2. Dynamic Pagination, or paging the tables on the server side to increase page speed on larger databases (not having to loop over 10,000 rows to only actually show 10).

3. Ability to click on each column header in order to choose which element to sort by and what direction (ascending or descending).

4. Something I already added, but a clear button to clear filters (so you can see the base table).

5. Ability to choose which columns are displayed (so you choose to also see the price of each product when looking at their totals without having to see everything else.